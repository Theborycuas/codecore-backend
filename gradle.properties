org.gradle.parallel=false
org.gradle.configuration-cache=false
# Reduces file locks on Windows when IDE and Gradle daemon watch build outputs.
org.gradle.vfs.watch=false
# Avoid concurrent test workers fighting over test-results/binary on Windows.
org.gradle.workers.max=1
