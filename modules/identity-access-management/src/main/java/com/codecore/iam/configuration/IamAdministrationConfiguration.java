package com.codecore.iam.configuration;

import org.springframework.context.annotation.Configuration;

/**
 * IAM administration layer entry point (FASE 15). Controllers under {@code interfaces.http.admin} are
 * component-scanned from {@code com.codecore.iam} via {@code CodeCoreApiApplication}.
 */
@Configuration
public class IamAdministrationConfiguration {
}
